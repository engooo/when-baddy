{
  "name": "when-baddy",
  "version": "0.1.0",
  "description": "Sydney badminton court finder with live availability, pricing, date switching, and booking links",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w backend && npm run build -w frontend",
    "start": "npm start -w backend",
    "scrape": "npm run scrape -w backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
